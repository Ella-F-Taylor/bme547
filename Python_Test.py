#first python file test
x = [1, 2, 3, 4, "banana"]

#to access all of the variables like Spyder could, click the outline tab on the lower left
#then run the code using run current file in interactive window
#then look at the jupyter tab on the bottom

import numpy as np
y = np.array([5, 6, 7, 8, 9])
print(np.__version__)

